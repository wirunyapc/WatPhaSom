@model IEnumerable<Models.Entities.Product>

@{
    ViewBag.Title = "manageProduct";
}

<h2>manageProduct</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>

        </th>
        <th>
            @Html.DisplayNameFor(model => model.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.priceRetail)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.priceWholesale)
        </th>

             
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            <image src="@Url.Content(item.photoPath)" alt="item.name" height="100" width="100" />
        </td>

        <td>
            @Html.DisplayFor(modelItem => item.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.priceRetail)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.priceWholesale)
        </td>

       
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.productId }) |
            @Html.ActionLink("Details", "Details", new { id=item.productId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.productId })
        </td>
    </tr>
}

</table>
